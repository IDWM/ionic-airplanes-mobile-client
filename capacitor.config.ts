import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-airplanes-mobile-client',
  webDir: 'www'
};

export default config;
